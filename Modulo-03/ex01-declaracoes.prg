Function main()


     local cNome:='kauan'
     local nSalario:=2500
     local lAtivo:=.t.
     LOCAL dAdimi 
     local nCodigo:=23


    Set date British
    
    dAdimi:=ctod("20/09/2024")

    Qout("nome:" + cNOme)
    Qout("salario bruto:" + alltrim(str(nSalario)))
    Qout("em atividade? " , lAtivo)
    Qout("codigo:" + alltrim(str(nCodigo))) 
    QOut("data:" , dAdimi )

Return Nil   