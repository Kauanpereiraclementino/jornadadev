function main()

   local nTotal :=0 
   local nValor
   local nQtd:=0 

   

   while .T. 

    input "Digite os numeros para a soma:" to nValor
    if nValor ==0
        exit
    end if
     nTotal=nTotal + nvalor 
     nQtd=nQtd + 1 

end do

QOut("o valor dos numeros é :" + Alltrim(str(nTotal)))
Qout("a quantidade digitada é: " + Alltrim(str(nQtd)))



return nil 