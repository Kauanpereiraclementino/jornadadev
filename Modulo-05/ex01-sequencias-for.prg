function main()

    local nI:= 1 
    local nk
    local nl

    for nI:= 1 to 100

     QOut("sequencia :" + AllTrim(Str(nI)))
    
    next 
    
    for  nk:= -50 to 50 
        Qout ("sequencia:" + Alltrim(Str(nk)))

    next   

    for nl:= 80 to 5 step -1 
        Qout("Sequencia descrescente:" + alltrim(Str(nl)))

    next

return nil