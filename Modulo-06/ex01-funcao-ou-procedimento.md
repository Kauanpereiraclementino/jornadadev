## Exercício 1 — Função ou procedimento? 

# Str() :Funçao ,por que ele converte um numero em forma de string
# QOut(): procedimento ele so retorna algo se for atribuido a ele 
# Date(): Funçao ele retorna a data 
# Len():Funçao,por que  ele devolve  o tamanho do array
# Upper():procedimento,ele so serve para estilizar o texto 
# ClearScreen():procedimento,ele so limpa a tela nao faz nada util 

